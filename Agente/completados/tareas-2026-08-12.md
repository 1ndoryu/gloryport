# Tareas completadas — 2026-08-12

## 128A-1 — GLORYPORT v1 (bandeja + CLI, Rust/Win32)

**Qué se hizo**: creación desde cero de GLORYPORT en `C:\Users\Owner\OneDrive\Documentos\area-trabajo\GLORYPORT`,
una herramienta solo Windows tipo port-killer con bandeja del sistema, minimalista y de bajo
consumo: escáner `GetExtendedTcpTable` (IPv4+IPv6), kill por PID con `OpenProcess`/
`TerminateProcess`, menú de bandeja con notificaciones, auto-inicio HKCU, single-instance,
CLI `list`/`kill`/`--version`, icono embebido y documentación (arquitectura, roadmap, plan).

**Archivos**:

- `src/main.rs`, `src/cli.rs`, `src/ports.rs`, `src/process.rs`, `src/tray.rs`,
  `src/autostart.rs`, `src/icon.rs`
- `src/bin/hold_port.rs` (helper de pruebas), `tests/cli.rs` (E2E)
- `Cargo.toml`, `Cargo.lock`, `.gitignore`
- `docs/arquitectura.md`, `README.md`, `roadmap.md`, `AGENTS.md`
- `assets/gloryport.ico` (embebido), `assets/gloryport.png`, `tools/make-icon.ps1`

**Evidencia / gate** (todo verde):

| Chequeo | Resultado |
|---|---|
| `cargo fmt --check` | OK |
| `cargo clippy --all-targets -- -D warnings` | OK |
| `cargo test` | 13/13 (10 unit + 3 E2E) |
| E2E funcional | helper ocupa puerto → `list` lo ve → `kill` lo libera |
| Smoke bandeja | ventana `GloryPortTrayWnd` creada (hwnd real), proceso vivo, 2.ª instancia sale sola, cierre limpio |
| RAM bandeja | WorkingSet ~10.040 KB (varía ~10–17 MB según muestra), privada ~1,5–2,2 MB, 3–4 hilos |
| `gloryport list` release | ~44 ms, 26 filas (cmd) |
| Binario release | 196.608 bytes (~192 KB), autocontenido |
| CLI release | `--version`, `list`, `list --json` correctos vía `cmd`; JSON de 25 objetos parseado |

**Decisiones / gotchas**:

- Stack elegido: Rust + crate `windows` 0.62.2 (features acotadas), un solo binario con
  `windows_subsystem = "windows"`; sin tokio, sin Electron, sin procesos externos.
- Escaneo bajo demanda (menú o CLI), sin timers: CPU ~0 % en reposo.
- Caché de nombres de proceso con TTL 10 s y tope 512 entradas.
- Kill protegido: nunca termina PID 0, PID 4 (System) ni el propio proceso.
- Salida tolerante a broken pipe (macros `outln!`/`errln!` en `cli.rs`): PowerShell cierra la
  tubería de un binario de subsistema gráfico y un `println!` estándar paniqueaba
  (os error 232); ahora la escritura se omite sin panico. En `cmd` y en llamadas
  programáticas la salida es completa.
- Compresión IPv6: `ipv6_to_string` devolvía `[]` para `[0;16]`; corregido para emitir
  `[::]` (caso cubierto por test unitario).
- Revisión de supervisor (`supervisor_reviewer`): **aprobar con observaciones**; se
  aplicaron en el mismo bloque: item "Actualizar lista" ahora re-escanea y reabre el menú
  (antes era no-op), cifra de RAM documentada con rango real (privada vs WorkingSet) y
  mejoras P2/P3 registradas en el roadmap (`128A-5`, `128A-6`).
- El shim `cargo` del entorno usa `C:\tmp\glory-target` como target dir (ruta del binario
  release en esta máquina).

**Prevención / Sentinel**: no aplica gate declarado en este proyecto (AGENTS.md local define
fmt+clippy+test+E2E como gate canónico).

**Pendientes reales**: validación de uso prolongado de la bandeja; fases P5/P6 del roadmap
(puertos vigilados, instalador/firma, cmdline en tooltip).

## 128A-7 — Popup Wispr Flow (bandeja) y fix del clic físico del icono

**Qué se hizo**: el menú nativo (`TrackPopupMenu`) se reemplazó por un popup propio
estilizado con GDI siguiendo la referencia Wispr Flow (paleta `#ffffeb`/`#1a1a1a`/
`#f0d7ff`/`#034f46`/`#e4e4d0`, fuentes Figtree 400/500/600 + EB Garamond 400 embebidas con
`AddFontMemResourceEx`, layout compacto de 300 px, máx. 8 filas visibles con scroll de
rueda, insignia `N TCP`/`N+ TCP`). Se corrigió el bug por el que el clic físico en el icono
no abría el popup y se añadió robustez a la bandeja.

**Archivos**:

- `src/popup.rs` (nuevo) — popup GDI: layout, hit-test, hover, scroll, acción por clic,
  cierre por Esc / segundo clic / pérdida de foco.
- `src/fonts.rs` (nuevo), `assets/fonts/` (TTFs + OFL), `tools/make-fonts.py` (nuevo).
- `src/tray.rs` — usa `popup::show`, fix de `lParam` (palabra baja), re-agrega icono en
  `TaskbarCreated`, sin `SetCapture`.
- `src/main.rs` — DPI per-monitor v2 y módulos `fonts`/`popup`.
- `src/ports.rs`, `Cargo.toml` — se retira `menu_label`; features Win32 de UI.
- `assets/popup.png`, `README.md`, `docs/arquitectura.md`, `AGENTS.md`.

**Evidencia / gate** (todo verde):

| Chequeo | Resultado |
|---|---|
| `cargo fmt` + `cargo clippy --all-targets -- -D warnings` | OK |
| `cargo test` | 20/20 OK (17 unit —incluye layout/hit-test del popup— + 3 E2E) |
| `cargo build --release` | OK, 348.160 bytes |
| Smoke físico (release) | clic real sobre el icono vía `SendInput` → popup visible en ~25 ms, estable a 300 ms, 2.º clic cierra sin reabrir |
| Clic derecho | alterna el popup igual que el izquierdo (`WM_CONTEXTMENU`) |

**Decisiones / gotchas**:

- Con `NOTIFYICON_VERSION_4`, `lParam` del callback de bandeja = `(uID << 16) | mensaje`;
  el handler compara `lParam & 0xFFFF`. El bug pasó los tests por `PostMessage` con
  `lParam` limpio (sin id en palabra alta) mientras el clic real iba empaquetado.
- El popup **no captura el ratón**: con captura, el 2.º clic en el icono cerraba el popup y
  el `WM_LBUTTONUP` del shell lo reabría al instante. Sin captura, el 2.º clic alterna y el
  cierre por clic fuera se delega a `WM_ACTIVATE` (`WA_INACTIVE`), como menú nativo.
- `SendInput` para smoke: las coordenadas absolutas van normalizadas (0–65535) y el flag de
  movimiento es `MOUSEEVENTF_MOVE|ABSOLUTE` (0x8001); un test con píxeles crudos movía el
  cursor a otro sitio y no ejercitaba el icono.
- `TaskbarCreated` (mensaje registrado) re-agrega el icono si Explorer se reinicia; los
  iconos fantasma de procesos terminados a la fuerza siguen siendo fricción del shell.

**Prevención / Sentinel**: no aplica gate declarado en el proyecto. Fricción registrada en
roadmap `128A-8` (iconos fantasma tras cierres forzados).

**Pendientes reales**: `128A-8` — mitigación de iconos fantasma en la bandeja; validación
del popup en uso diario (DPI 125/150 %, alto contraste).

## 128A-10 — Popup más espacioso sin título/contador + filtro solo-aplicaciones

**Qué se hizo**: el popup de bandeja dejó de ser compacto: ahora mide 340×474 px con filas
de 34 px (antes 300 px / 30 px), más aire en paddings, hasta 9 filas visibles y **sin
cabecera ni insignia de contador**. Se eliminó el misterio de "desconocido": cada proceso se
resuelve con `QueryFullProcessImageNameW` (ruta completa del ejecutable) y el popup aplica un
filtro **solo-aplicaciones** (puerto ≥ 1024 y ejecutable fuera de `C:\Windows`), de modo que
los servicios del sistema y los procesos sin nombre resoluble ya no aparecen. Se corrigió
además la carrera por la que el segundo clic cerraba el popup y lo reabría al instante.

**Archivos**:

- `src/popup.rs` — layout amplio (340×474), sin cabecera/insignia, PID con elipsis,
  `record_close()`/`closed_recently()` para suprimir la reapertura del 2.º clic + test.
- `src/ports.rs` — `PortInfo.process_path`, caché de nombre+ruta, `solo_aplicaciones()` +
  test; `list --incluir-sistema`.
- `src/process.rs` — `resolve_process_path` con `PROCESS_QUERY_LIMITED_INFORMATION`; se
  retira `resolve_process_name` (dead code).
- `src/tray.rs` — supresión de 250 ms en `toggle_or_show_menu` (carrera cerrar→reabrir).
- `src/cli.rs` — `list` filtra aplicaciones por defecto; `--incluir-sistema` para ver todo.
- `tools/smoke-tray.ps1` (nuevo) — smoke reproducible con `SendInput` + UIA + captura PNG +
  sonda HWND/rect para distinguir cierre de reapertura.
- `assets/popup.png`, `README.md`, `docs/arquitectura.md`, `AGENTS.md`.

**Evidencia / gate** (todo verde):

| Chequeo | Resultado |
|---|---|
| `cargo fmt --check` | OK |
| `cargo clippy --all-targets -- -D warnings` | OK |
| `cargo test` | 21/21 OK (18 unit —incluye filtro y supresión— + 3 E2E) |
| `cargo build --release` | OK, 350.208 bytes |
| Smoke físico (release) | icono UIA → popup 340×474 visible en < 130 ms → captura PNG → 2.º clic cierra sin reabrir (HWND idéntico eliminado, sin reemplazo) |
| `gloryport list` (release) | solo aplicaciones: node/bun/Code/GoogleDriveFS/esrv/Freebuff; `--incluir-sistema` añade 135/139/445/5040/5432/PID 4, etc. |

**Decisiones / gotchas**:

- El filtro es "aplicaciones de usuario" (puerto ≥ 1024 + ruta fuera de `C:\Windows`), no
  "solo la carpeta `area-trabajo`": `node.exe`/`bun.exe` viven en `Program Files` y un
  filtro por carpeta ocultaría los servidores de los proyectos.
- "Desconocido" era un proceso muerto entre el escaneo y la consulta del nombre, o un
  servicio sin acceso de lectura (SYSTEM/otro usuario). Con la ruta resuelta y el filtro
  aplicado no aparecen en el popup; en `list --incluir-sistema` siguen como "desconocido".
- Carrera del 2.º clic (verificada con sonda HWND): el DOWN sobre la bandeja desactiva el
  popup (cierre por `WM_ACTIVATE`) y el UP posterior llega al callback de bandeja cuando el
  popup ya está cerrado, reabriéndolo con otro HWND en la misma posición. Fix: ventana de
  supresión de 250 ms desde el último cierre; "Actualizar lista" no se ve afectado (reabre
  por vía interna, no por clic de bandeja).
- `tools/smoke-tray.ps1` debe ser ASCII puro: PowerShell 5.1 lee el archivo como ANSI y un
  carácter UTF-8 (p. ej. "—" u "ó") rompe el parseo o produce mojibake.

**Prevención / Sentinel**: no aplica gate declarado en el proyecto.

**Pendientes reales**: validación del popup en uso diario; `128A-9` reduce a declarar
`rust-version` en `Cargo.toml` (la elipsis del PID quedó resuelta en este bloque); fases
P5/P6 del roadmap.

## 128A-12 — Popup sin Salir/Acerca, cursor flecha, blocklist, cmdline node/bun y elipsis al inicio

**Qué se hizo**: bloque de pulido del popup y de la identificación de procesos. Se
eliminaron los botones "Acerca de" y "Salir" del popup (el cierre queda solo en el segundo
clic del icono o clic fuera; aún no hay vía de salida por UI — hoy se detiene con
`Stop-Process`/Administrador de tareas, vía pendiente en `128A-14`), se arregló el cursor de
"cargando" que aparecía al pasar por el popup, se añadió una blocklist de procesos
(GoogleDriveFS) que nunca se ofrecen al kill, y los intérpretes node/bun ahora se
identifican por su **línea de comandos real** (script que ejecutan) en vez de `node.exe`
genérico, sin hardcodear puertos→aplicación. Por último, cuando la ruta no cabe en la fila,
se recorta por el **principio** (se ve el final, no el comienzo).

**Archivos**:

- `src/popup.rs` — sin `About`/`Exit` (Action/Hit/layout/hit_test/footer; altura 340×400),
  cursor `IDC_ARROW` en la clase + `WM_SETCURSOR` (fix cursor de espera),
  `text_leading_ellipsis()`/`sufijo_con_elipsis()` (elipsis al inicio por búsqueda binaria
  + test).
- `src/process.rs` — `resolve_process_cmdline(pid)` vía `NtQueryInformationProcess` (PEB
  x64) + `ReadProcessMemory` + test del propio proceso.
- `src/ports.rs` — `PortInfo.process_cmd`, `PROCESOS_EXCLUIDOS` (GoogleDriveFS) en
  `es_puerto_de_aplicacion`, `etiqueta_visible()` (script de intérpretes con
  `primer_script`/`normalizar_ruta`/`acortar_ruta`) + 2 tests.
- `src/tray.rs` — sin brazos About/Exit; notificación de kill con `etiqueta_visible`.
- `src/cli.rs` — columna PROCESO usa `etiqueta_visible`; `process_cmd` sale en `--json`.
- `Cargo.toml` — feature `Win32_System_Diagnostics_Debug`.
- `README.md`, `docs/arquitectura.md`, `roadmap.md`, `assets/popup.png`.

**Evidencia / gate** (todo verde):

| Chequeo | Resultado |
|---|---|
| `cargo fmt --check` | OK |
| `cargo clippy --all-targets -- -D warnings` | OK |
| `cargo test` | 24/24 OK (21 unit —incluye cmdline propia, etiqueta node/bun, blocklist y elipsis— + 3 E2E) |
| `cargo build --release` | OK |
| Smoke físico (release) | popup 340×400 visible en < 130 ms; 2.º clic cierra sin reabrir |
| Verificación cursor | sonda `GetCursorInfo` sobre el popup abierto: clase `GloryPortPopupWnd`, cursor `ARROW` (no wait) |
| `gloryport list` (release) | `3101 → …\server\dist\index.js`, `4100 → …\codex-bridge\bridge\server.js`, `5173 → …\vite\bin\vite.js`, `50086 → …\resources\orchestrator\orchestrator.js` (bun Freebuff); GoogleDriveFS ya no aparece |
| Render del popup (OCR) | las filas de rutas largas terminan en `server.js` / `orchestrator.js` (cola conservada, no cortada) |

**Decisiones / gotchas**:

- `DT_BEGINNING_ELLIPSIS` **no existe** en Win32 (solo `DT_END_ELLIPSIS` y
  `DT_PATH_ELLIPSIS`); se implementó el recorte manual midiendo con
  `GetTextExtentPoint32W` y buscando por búsqueda binaria la cola más larga que quepa.
- La cmdline se lee del PEB (offsets x64: `ProcessParameters` en `+0x20`, `CommandLine`
  `UNICODE_STRING` en `+0x70`) sin WMI ni hijos; los procesos de sistema/otro usuario dan
  `None` y el filtro de aplicaciones los oculta igualmente. Sin elevación.
- No hay mapa puerto→aplicación: la etiqueta se deriva del proceso real de cada escaneo
  (`primer_script` con extensiones js/mjs/cjs/ts/mts/cts), porque una app puede ocupar
  puertos distintos en días distintos.
- El cursor de espera venía de la clase sin `hCursor` (el hilo heredaba el cursor previo);
  se fija `IDC_ARROW` en la clase y `WM_SETCURSOR` devuelve 1 con `SetCursor(arrow)`.
  `HCURSOR` no es `Sync`: se guarda raw (`usize`) en `OnceLock` y se reconstruye.
- GoogleDriveFS cumple el filtro de aplicación (puerto alto, ruta fuera de Windows), por
  eso se añadió `PROCESOS_EXCLUIDOS` (case-insensitive por nombre/basename).
- `esrv.exe` (puerto 49351) es un componente Intel (SUR/QUEENCREEK), no un proyecto del
  usuario; se mantiene visible porque es una aplicación real y su etiqueta es el nombre.

**Prevención / Sentinel**: no aplica gate declarado en el proyecto.

**Pendientes reales**: `128A-5` (re-verificar PID antes del kill), `128A-9`
(`rust-version`), `128A-13` (IPv4 mostrado invertido en `list`, preexistente),
`128A-14` (vía de salida por UI + números del README), validación del popup en uso
diario; fases P5/P6 del roadmap.

## 128A-15 — Tooltip con ruta completa, popup 430×436 y clic derecho en la bandeja

**Qué se hizo**: el tooltip clásico no aparecía sobre las filas; se reimplementó como
tooltip "track" (`TOOLTIPS_CLASSW` hijo del popup con `TTS_ALWAYSTIP|TTS_NOPREFIX`,
`TTF_IDISHWND|TTF_TRACK|TTF_ABSOLUTE` y `LPSTR_TEXTCALLBACK`) que se activa/posiciona
explícitamente en `set_hover` y sirve la ruta completa por `TTN_NEEDTEXTW`. El popup vuelve
a ser amplio (430×436, filas de 38 px) y `draw_row` mide la etiqueta con
`GetTextExtentPoint32W`: si cabe completa se dibuja entera; si no, elipsis al inicio
conservando el final. `tray.rs` reenvía `WM_RBUTTONUP` a la misma vía que
`WM_LBUTTONUP | WM_CONTEXTMENU` para que el clic derecho también abra el popup.

**Archivos**: `src/popup.rs` (tooltip track + tamaño 430×38 + ruta completa si cabe),
`src/tray.rs` (`WM_RBUTTONUP`), `.gitignore` (`.cargo/config.toml`), `README.md`,
`docs/arquitectura.md`, `roadmap.md`.

**Evidencia / gate** (todo verde):

| Chequeo | Resultado |
|---|---|
| `cargo fmt` | OK |
| `cargo clippy --all-targets -- -D warnings` | OK |
| `cargo test` | 24/24 OK (21 unit + 3 E2E) |
| `cargo build --release` | OK, `gloryport.exe` 357.888 bytes |

**Decisiones / gotchas**:

- El tooltip clásico (subclase automática del popup) jamás aparecía; el plan robusto es
  un tooltip "track" que se posiciona con `TTM_TRACKPOSITION` y se activa/desactiva con
  `TTM_TRACKACTIVATE` desde `set_hover`, sin depender del subclaseado.
- `TTM_SETMAXTIPWIDTH=0` desactiva el wrap: la ruta completa sale en una sola línea.
- `lpszText=LPSTR_TEXTCALLBACK` es obligatorio (con `NULL` el tooltip no aparece) y
  `TTN_NEEDTEXTW` copia al buffer fijo del mensaje (sin punteros a memoria propia).
- La `TTTOOLINFOW` se guarda en `PopupState` con `unsafe impl Send` (solo se toca desde el
  hilo de la UI) y se destruye antes del `DestroyWindow` del popup.
- Se perdió por un checkout el tamaño 430×38 y la lógica "ruta completa si cabe"; quedó
  re-aplicado en este bloque y verificado por el gate.

**Prevención / Sentinel**: no aplica gate declarado en el proyecto.

**Pendientes reales**: prueba física del usuario (tooltip en hover, clic izq./der. abre);
después `128A-5`, `128A-9`, `128A-13`, `128A-14` y validación de uso diario.
